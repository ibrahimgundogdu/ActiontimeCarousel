@model EmployeeControlModel

<div class="table">
    <table class="table table-sm table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Lokasyon</th>
                <th>Tarih</th>
                <th>Birim Fiyat</th>
                <th>Çalışma Süresi</th>
                <th>Saatlik Hakediş</th>
                <th>Hakediş</th>
                <th>Set Card Çarpan</th>
                <th>Set Card Hakediş</th>
                <th>Para Birimi</th>
            </tr>
        </thead>
        <tbody>

            @{


            }


            @foreach (var item in Model.SalaryEarn)
            {
                //Model.FooterTotals.FirstOrDefault(x => x.Currency == item.Currency).Total += item.Amount ?? 0;
                //Model.MiddleTotals.FirstOrDefault(x => x.Currency == item.Currency).Total += item.Amount ?? 0;

            <tr>

                <td class="tx-sm-13 bold">@item.LocationID</td>
                <td class="tx-sm-13">@item.LocationFullName</td>

                <td class="tx-sm-13">@(item.Date?.ToLongDateString())</td>

                <td class="tx-sm-13">@(item.UnitPrice?.ToString("N2"))</td>
                <td class="tx-sm-13">@(item.QuantityHour?.ToString("N2"))</td>
                <td class="tx-sm-13 font-weight-bold">@(item.QuantityHourSalary?.ToString("N2"))</td>

                <td class="text-left bg-light font-weight-bold">@((item.UnitPrice * item.QuantityHourSalary)?.ToString("N2"))</td>
                <td class="tx-sm-13 font-weight-bold">@(item.QuantityHourFood?.ToString("N4"))</td>
                <td class="text-left bg-warning font-weight-bold">@((item.QuantityHourFood * Model.SetcardParameter.EarnHour * Model.SetcardParameter.Amount)?.ToString("N4"))</td>
                <td class="tx-sm-13">@item.Currency</td>


            </tr>
            }


        </tbody>
        <tfoot>


            @*Genel Toplam*@
            <tr class="bg-light">
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th colspan="1">Genel Toplam</th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "TRL").Amount.ToString("N2"))</th>
                <th></th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "TRL").Total.ToString("N4"))</th>
                <th>TRL</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
            <tr class="bg-light">
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th colspan="1">Genel Toplam</th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "USD").Amount.ToString("N2"))</th>
                <th></th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "USD").Total.ToString("N4"))</th>

                <th>USD</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
            <tr class="bg-light">
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th></th>
                <th colspan="1">Genel Toplam</th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "EUR").Amount.ToString("N2"))</th>
                <th></th>
                <th class="text-right bg-dark font-weight-bold text-white">@(Model.FooterTotal.FirstOrDefault(x => x.Currency == "EUR").Total.ToString("N4"))</th>
                <th>EUR</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
        </tfoot>
    </table>
</div>